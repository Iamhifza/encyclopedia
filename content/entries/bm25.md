---
term: BM25
aliases: [Okapi BM25, Best Matching 25, Lexical Ranking]
category: rag-knowledge
subcategory: retrieval
depth: full
status: foundational
difficulty: intermediate
one_liner: "The keyword ranking function that scores documents by rare-term overlap, still a strong baseline half a century after it was designed."
origin:
  year: 1994
  circa: true
  attribution: Robertson and Sparck Jones' probabilistic relevance framework; the Okapi system gave it its name
historical_period: statistical-ml
diagram:
  kind: steps
  title: Rare words count, and repetition stops counting
  footer: Twenty-five years old, still the baseline a new retrieval method has to beat, and still the
    thing that finds an exact error code when a vector model cannot.
  steps:
  - title: A term is worth what its rarity says it is worth
    notes:
    - label: IDF
      text: inverse document frequency — a word in every document distinguishes nothing
    visual:
      kind: bars
      caption: 'query: "kv cache fragmentation"'
      bars:
      - label: kv
        value: 0.92
        value_label: rare · high IDF
        accent: true
      - label: cache
        value: 0.44
        value_label: common · moderate
      - label: fragmentation
        value: 0.88
        value_label: rare · high IDF
  - title: The twentieth mention barely counts
    notes:
    - label: Saturation
      text: term frequency is damped, so a page that repeats a word cannot buy its way to the top
    - label: Length
      text: and the score is normalised by document length, so long documents get no free advantage
    visual:
      kind: plot
      width: 660
      height: 190
      x_range: [0, 20]
      y_range: [0, 1.1]
      x_label: occurrences in the document
      y_label: contribution
      caption: the curve flattens fast; most of the value is in the first two or three mentions
      curves:
      - label: BM25
        tone: accent
        points: [[0.0, 0.0], [0.5, 0.272], [1.0, 0.435], [1.5, 0.543], [2.0, 0.621], [2.5, 0.679], [3.0,
            0.725], [3.5, 0.761], [4.0, 0.791], [4.5, 0.815], [5.0, 0.836], [5.5, 0.854], [6.0, 0.87],
          [6.5, 0.883], [7.0, 0.895], [7.5, 0.906], [8.0, 0.915], [8.5, 0.924], [9.0, 0.932], [9.5, 0.939],
          [10.0, 0.945], [10.5, 0.951], [11.0, 0.957], [11.5, 0.962], [12.0, 0.966], [12.5, 0.97], [13.0,
            0.975], [13.5, 0.978], [14.0, 0.982], [14.5, 0.985], [15.0, 0.988], [15.5, 0.991], [16.0,
            0.994], [16.5, 0.996], [17.0, 0.999], [17.5, 1.001], [18.0, 1.003], [18.5, 1.005], [19.0,
            1.007], [19.5, 1.009], [20.0, 1.011]]
      - label: raw count
        tone: muted
        points: [[0, 0], [20, 1.05]]
tags: [retrieval]
relations:
  part_of: [information-retrieval]
  successor_of: [tf-idf]
  alternative_to: [dense-retrieval]
  used_by: [hybrid-retrieval, rag]
prerequisites: [information-retrieval]
encountered_in: [production-systems, research-papers, interviews, github]
sources:
  - type: paper
    title: "The Probabilistic Relevance Framework: BM25 and Beyond"
    url: https://www.staff.city.ac.uk/~sbrp622/papers/foundations_bm25_review.pdf
    year: 2009
  - type: book
    title: "Introduction to Information Retrieval, ch. 11"
    url: https://nlp.stanford.edu/IR-book/
    year: 2008
updated: 2026-08-21
---

## Simple Explanation

Score a document by which of the query's words it contains, weighting rare words
more heavily than common ones, discounting repeated occurrences, and correcting
for document length so long documents do not win by sheer size.

That is the whole idea, it was settled in the 1990s, and it is still the default
in every serious search system.

## Technical Definition

A probabilistic ranking function scoring a document $d$ against a query $q$ by
summing, over query terms, an inverse-document-frequency weight multiplied by a
saturating function of term frequency, normalised by document length relative to
the collection average.

## Why Does It Exist?

TF-IDF weighted terms sensibly but had two defects: term frequency grew without
limit, so a document repeating a word fifty times scored absurdly high, and long
documents accumulated matches simply by being long. BM25 fixes both with two
tunable parameters.

## What Problem Does It Solve?

Ranking by lexical relevance in a way that survives real corpora, where documents
vary enormously in length and term frequency is a noisy signal.

## Formula

$$\text{score}(q,d) = \sum_{t \in q} \text{IDF}(t) \cdot \frac{f(t,d)\,(k_1+1)}{f(t,d) + k_1\left(1 - b + b\frac{|d|}{\text{avgdl}}\right)}$$

* $f(t,d)$ — how often term $t$ appears in document $d$.
* $\text{IDF}(t)$ — inverse document frequency; a term in every document carries
  almost no signal, a rare one carries a great deal.
* $|d|$, $\text{avgdl}$ — this document's length and the collection average.
* $k_1$ — **saturation**, typically 1.2–2.0. The tenth occurrence of a word adds
  far less than the second. This is the fix TF-IDF lacked.
* $b$ — **length normalisation**, typically 0.75. At $b=1$ length is fully
  corrected for; at $b=0$ not at all.

## How Does It Work?


Score a document against a query by adding up a contribution per query term. Each
term's contribution is its inverse document frequency — how rare it is across the
corpus — multiplied by how often it appears in this document, with two
corrections that are the whole reason BM25 outlived plain TF-IDF.

The first is saturation. Raw term frequency rewards repetition without limit, so
a page that says "cache" forty times outranks a page that explains caching. BM25
damps it: the first two or three occurrences carry most of the weight and the
curve flattens after that. The second is length normalisation, which stops long
documents scoring highly merely for containing more words.

Two parameters control it — *k₁* for how fast saturation sets in, *b* for how
strongly length is penalised — and the defaults are good enough that most systems
never touch them. What makes it durable is exactness: it finds a product code, an
error string or a rare proper noun that a dense retriever will miss entirely,
because it is matching tokens rather than meaning.

## Mental Model

A librarian who has memorised which words are unusual. Mentioning "fragmentation"
tells them a great deal; mentioning "the" tells them nothing.

## Example

BM25's persistence is the interesting fact about it. Neural retrieval papers are
still expected to benchmark against it, and it still sometimes wins —
particularly out of domain, where a dense encoder trained elsewhere degrades and
BM25, which was never trained at all, does not. Its failure is equally
predictable: no shared vocabulary, no match. "My card got declined" finds nothing
in a document titled "payment authorisation failures".

## Real-World Usage

The default ranking function in Lucene, Elasticsearch, OpenSearch, Vespa and most
search infrastructure — usually one configuration flag away. In RAG stacks it is
the lexical half of hybrid retrieval, and omitting it is one of the most common
causes of a pipeline that cannot find an error code.

## Common Confusions

* **BM25 is not "just keyword search"** — the ranking is the whole point;
  boolean matching would return the same set unordered.
* **BM25 vs TF-IDF** — same intuition, better mechanics. Saturation and length
  normalisation are what make it work on real collections.
* **Parameters are rarely the problem** — defaults are good. If BM25 is
  underperforming, look at analysis and tokenisation first.

## Why Should I Care?

It is the strongest evidence in this encyclopedia that older is not worse: a
ranking function from the 1990s remains a component of state-of-the-art retrieval
systems, and leaving it out is a common and avoidable mistake.
